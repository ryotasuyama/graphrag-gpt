# ブラケット生成リトライ改善 実装計画

## 背景・課題

### 根本原因（0310-2/4 の分析より）
Agent 1（分析）が **Fore/Aft の方向整合を無視したブラケット候補** を生成すると、
Agent 2（コード生成）がそのまま不正なコードを出力し、COM例外が発生する。
リトライは Agent 2 だけを再実行するため、**同じ analysis.md の間違った候補を何度もコード化**してしまう。

具体例:
- `profile121`（Wall.**Aft**.DL00） × `profile93`（Deck.D.DL00.**F**） → 物理的に交差しない → COM例外
- `profile63`（Wall.**Aft**.DL03） × `profile54`（Deck.C.DL03.**FP**） → 同上

正解:
- `profile121`（Wall.Aft.DL00） × `profile33`（Deck.D.DL00.**A**） → Aft同士で正しいペア

### 0310-2 と 0310-3 の決定的な違い

| ケース | bracket24 の Surfaces2 | 結果 |
|--------|----------------------|------|
| 0310-2（失敗） | `profile93` = Deck.D.DL00.**F** | Aft壁 × Fore側Deck → COM例外 |
| 0310-3（成功） | `profile33` = Deck.D.DL00.**A** | Aft壁 × Aft側Deck → 成功 |

### 現在のリトライフローの構造的問題

```
Agent 1 → analysis.md（ここでペアリングミスが入る）
    ↓
Agent 2 → コード生成 → 実行 → COM例外
    ↓
Agent 2（リトライ1）→ 同じ analysis.md + エラー情報 → 同じ間違い
    ↓
Agent 2（リトライ2）→ 同じ analysis.md + エラー情報 → 同じ間違い
    ↓
Agent 2（リトライ3）→ 同じ analysis.md + エラー情報 → 同じ間違い → 最終失敗
```

**問題点:**
1. リトライ時に Agent 1（分析）は再実行されない — 間違った analysis.md がそのまま使われ続ける
2. COM例外は汎用的で原因不明 — `pywintypes.com_error (-2147467259)` からは空間的な誤りが分からない
3. リトライ LLM は変数名（`profile121`）しか見えず、部材名（`Wall.Aft.DL00.CD`）が見えない

---

## 施策2: 分析プロンプトにルール追加

### 概要
Agent 1 が使う分析ガイドに **Fore/Aft 方向整合ルール** を追加し、不正な候補を初回生成時に排除する。

### 対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `prompts/bracket_analysis_guide.md` | 方向整合ルールセクション追加（ルール4） |
| `prompts/bracket_analysis_guide_noref.md` | 同上（セクション8として追加） |

### 追加するルール内容

#### `bracket_analysis_guide.md` — 末尾に追加

```markdown
4. **Fore/Aft 方向整合ルール（必須）**
   - ProfileName のサフィックスから方向を判定し、以下のペアリングのみ許可すること:
     - `Wall.Fore.DLxx.*` → `Deck.X.DLxx.F` / `Deck.X.DLxx.FP` のみ
     - `Wall.Aft.DLxx.*`  → `Deck.X.DLxx.A` / `Deck.X.DLxx.AP` のみ
   - あるDL線に片側（.F/.FPのみ or .A/.APのみ）のDeckプロファイルしか存在しない場合、
     反対側の壁プロファイルとのブラケットは **候補から除外** すること
   - ブラケット配置リスト作成時、各行で Wall 側と Deck 側の Fore/Aft が一致しているか必ず検証すること
   - Side Wall（FR系）の場合、方向制約はないが、接続先 Deck プロファイルの存在確認は必要
```

#### `bracket_analysis_guide_noref.md` — セクション7の後に追加

```markdown
### 8. Fore/Aft 方向整合ルール（必須）

ブラケット候補を確定する前に、以下の方向整合を必ず検証してください：

| 壁側プロファイル | 接続可能な Deck プロファイル | 接続不可 |
|----------------|---------------------------|---------|
| `Wall.Fore.DLxx.*` (CDP/BCP/ABP/OAP) | `Deck.*.DLxx.F` / `Deck.*.DLxx.FP` | `Deck.*.DLxx.A` / `Deck.*.DLxx.AP` |
| `Wall.Aft.DLxx.*` (CDP/BCP/ABP/OAP)  | `Deck.*.DLxx.A` / `Deck.*.DLxx.AP` | `Deck.*.DLxx.F` / `Deck.*.DLxx.FP` |
| `Wall.Side.FRxx.*` | 方向制約なし（Deck.*.FRxxP 等） | — |

**検証手順:**
1. 元スクリプトの全 `ProfilePram*.ProfileName` を一覧し、Fore/Aft を分類する
2. ブラケット配置リストの各行について、Surfaces1 側の部材名と Surfaces2 側の部材名を照合する
3. Fore壁×Aft側Deck、Aft壁×Fore側Deck のペアが見つかった場合は **その候補を除外**する
4. 除外した候補の代わりに、同じDL線の正しい方向の Deck プロファイルが存在すれば差し替える
5. 正しい方向の Deck プロファイルが存在しない場合は、そのDL線のブラケットは作成不可とする

**よくある間違い例:**
- ❌ `Wall.Aft.DL00.CD` (profile121) × `Deck.D.DL00.F` (profile93) → Aft × Fore は不可
- ✅ `Wall.Aft.DL00.CD` (profile121) × `Deck.D.DL00.A` (profile33) → Aft × Aft で正しい
- ❌ `Wall.Aft.DL03.BCP` (profile63) × `Deck.C.DL03.FP` (profile54) → Aft × Fore は不可
- ✅ `Wall.Fore.DL03.BCP` (profile53) × `Deck.C.DL03.FP` (profile54) → Fore × Fore で正しい
```

### 実装手順

1. `prompts/bracket_analysis_guide.md` を開き、3番目のルール（「配置の完全性とエラー回避」）の後に上記「4. Fore/Aft 方向整合ルール」を追加
2. `prompts/bracket_analysis_guide_noref.md` を開き、「### 7. 注意事項」セクションの後に「### 8. Fore/Aft 方向整合ルール」セクションを追加
3. テスト: 0310-2 の元スクリプトと同じ指示で Agent 1 を再実行し、analysis.md に不正ペアが含まれないことを確認

---

## 施策1: リトライ時に Agent 1 を再実行

### 概要
現在のリトライは Agent 2（コード生成）のみ再実行。
改善後は **Agent 1（分析）も再実行** し、エラー情報を踏まえて analysis.md を修正してから Agent 2 に渡す。

### 改善後のリトライフロー

```
Agent 1 → analysis.md → Agent 2 → code → execute → FAIL
    ↓
Agent 1（再分析 + エラー情報 + ProfileName逆引き）→ revised_analysis.md
    ↓
Agent 2（修正版 analysis で再生成）→ code → execute → FAIL/OK
    ↓（まだ失敗）
Agent 1（再分析 + 過去2回の履歴）→ revised_analysis.md → Agent 2 → code → execute
```

### 対象ファイル

| ファイル | 変更内容 |
|---------|---------|
| `query.py` L1160-1216 | リトライループに Agent 1 再実行ロジック追加 |
| `query.py` L835-845 | `reanalyze_bracket_placement()` 関数を新規追加 |
| `prompts/loader.py` L78-117 付近 | `build_reanalysis_prompt()` 等 4関数を新規追加 |
| `prompts/bracket_reanalysis.md`（新規） | Agent 1 リトライ用プロンプトテンプレート |

### 新規関数 1: `reanalyze_bracket_placement()`

場所: `query.py` L845 付近（`analyze_bracket_placement()` の直後）

```python
def reanalyze_bracket_placement(
    instruction: str,
    original_code: str,
    previous_analysis: str,
    error_context: dict,
    reference_code: Optional[str] = None,
    llm=None,
) -> str:
    """Agent 1 (リトライ): エラー情報を踏まえてブラケット配置分析を再実行"""
    print("--- [Agent 1 Retry] Re-analyzing bracket placement ---")
    prompt = build_reanalysis_prompt(
        instruction, original_code, previous_analysis, error_context, reference_code
    )
    response = llm.invoke(prompt)
    return response.content.strip()
```

### 新規関数 2: `build_reanalysis_prompt()`

場所: `prompts/loader.py` L117 付近（`build_analysis_prompt()` の直後）

```python
def build_reanalysis_prompt(
    instruction: str,
    original_code: str,
    previous_analysis: str,
    error_context: dict,
    reference_code: str = None,
) -> str:
    """Agent 1 (リトライ): エラーコンテキスト付きで分析プロンプトを構築"""
    parts = [load_prompt("base_system")]

    if reference_code:
        parts.append(load_prompt("bracket_analysis_guide"))
    else:
        parts.append(load_prompt("bracket_analysis_guide_noref"))

    # エラー診断セクション構築
    error_diagnosis = _build_reanalysis_error_section(error_context, original_code)

    template = load_prompt("bracket_reanalysis")
    question = template.format(
        instruction=instruction,
        original_code=original_code,
        previous_analysis=previous_analysis,
        error_diagnosis=error_diagnosis,
        reference_section=(
            f"【参考スクリプト】\n\n```python\n{reference_code}\n```\n"
            if reference_code else ""
        ),
    )

    parts.append(question)
    return "\n\n".join(parts)
```

### 新規関数 3: `_build_reanalysis_error_section()`

場所: `prompts/loader.py`（`_build_error_context_block()` の近く）

**この関数がキーポイント**: エラーコンテキストに **ProfileName** を付与する。

```python
def _build_reanalysis_error_section(error_context: dict, original_code: str) -> str:
    """Agent 1 再分析用のエラー診断セクションを構築（ProfileName 逆引き付き）"""

    # 1. スクリプトから ProfileName マッピングを構築
    profile_name_map = _extract_profile_name_map(original_code)
    #    例: {"profile121": "HK.Casing.Wall.Aft.DL00.CD", ...}

    # 2. 失敗したブラケットの関連変数から ProfileName を解決
    bracket_param_section = error_context.get("bracket_param_section", "")
    related_profiles = re.findall(r'(profile\d+)', bracket_param_section)
    profile_annotations = []
    for pvar in set(related_profiles):
        pname = profile_name_map.get(pvar, "不明")
        profile_annotations.append(f"  - {pvar} = `{pname}`")

    # 3. Fore/Aft 不整合の自動検出
    mismatch_warnings = _detect_fore_aft_mismatch(bracket_param_section, profile_name_map)

    # 4. 成功済みブラケット情報
    successful_count = error_context.get("successful_bracket_count", 0)
    failed_name = error_context.get("failed_bracket_name", "")

    section = f"""## エラー診断（Agent 1 再分析用）

### 前回分析の実行結果
- bracket 1〜{successful_count} は成功
- **{failed_name} で COM 例外が発生**

### 失敗したブラケットのパラメータ
```python
{bracket_param_section}
```

### 関連プロファイルの部材名
{chr(10).join(profile_annotations)}

{mismatch_warnings}

### エラートレースバック
```
{error_context.get("stderr", "")}
```

### 指示
1. 前回の分析ドキュメントのブラケット配置リストを見直してください
2. 上記の失敗ブラケットの候補を修正または除外してください
3. 成功した bracket 1〜{successful_count} はそのまま維持してください
4. 修正後の **完全なブラケット配置分析ドキュメント** を出力してください
"""
    return section
```

### 新規関数 4: `_extract_profile_name_map()`

場所: `prompts/loader.py` または `prompts/structure_extractor.py`

```python
def _extract_profile_name_map(code: str) -> dict:
    """スクリプトから ProfilePramN.ProfileName="..." を抽出してマッピングを返す"""
    mapping = {}
    for m in re.finditer(r'ProfilePram(\d+)\.ProfileName\s*=\s*"([^"]+)"', code):
        mapping[f"profile{m.group(1)}"] = m.group(2)
    return mapping
```

### 新規関数 5: `_detect_fore_aft_mismatch()`

場所: `prompts/loader.py`

```python
def _detect_fore_aft_mismatch(bracket_section: str, profile_name_map: dict) -> str:
    """ブラケットパラメータ内の Fore/Aft ペアリング不整合を検出"""
    profiles_in_section = set(re.findall(r'(profile\d+)', bracket_section))
    names = {pv: profile_name_map.get(pv, "") for pv in profiles_in_section}

    warnings = []
    wall_profiles = {pv: n for pv, n in names.items() if "Wall." in n}
    deck_profiles = {pv: n for pv, n in names.items() if "Deck." in n}

    for wpv, wname in wall_profiles.items():
        is_fore = ".Fore." in wname
        is_aft = ".Aft." in wname
        if not (is_fore or is_aft):
            continue
        for dpv, dname in deck_profiles.items():
            w_dl = re.search(r'DL\d+', wname)
            d_dl = re.search(r'DL\d+', dname)
            if not (w_dl and d_dl and w_dl.group() == d_dl.group()):
                continue
            d_is_fore = dname.endswith(".F") or dname.endswith(".FP")
            d_is_aft = dname.endswith(".A") or dname.endswith(".AP")
            if is_fore and d_is_aft:
                warnings.append(
                    f"  - ⚠ `{wpv}` ({wname}) は Fore壁だが "
                    f"`{dpv}` ({dname}) は Aft側Deck → 不整合"
                )
            elif is_aft and d_is_fore:
                warnings.append(
                    f"  - ⚠ `{wpv}` ({wname}) は Aft壁だが "
                    f"`{dpv}` ({dname}) は Fore側Deck → 不整合"
                )

    if warnings:
        return "### ⚠ Fore/Aft 方向不整合の検出\n" + chr(10).join(warnings)
    return ""
```

### 新規テンプレート: `prompts/bracket_reanalysis.md`

```markdown
## タスク（再分析モード）

前回のブラケット配置分析に基づいて生成したコードが実行時にエラーになりました。
以下の情報を基に、**修正版のブラケット配置分析ドキュメント**を出力してください。

**重要:**
- Pythonコードは一切出力しないこと。Markdownの仕様ドキュメントのみ出力してください
- 成功済みのブラケット候補はそのまま維持すること
- 失敗したブラケット候補のみ修正・除外すること
- Fore/Aft 方向整合ルールを再確認すること
- 候補数の要件（例:「20個以上」）を維持するよう、除外した候補の代替を提案すること

{reference_section}

{error_diagnosis}

【前回の分析ドキュメント】

{previous_analysis}

【元のスクリプト】

```python
{original_code}
```

-----

【編集指示】
{instruction}

-----

## 出力フォーマット（前回と同じ形式で出力してください）

## ブラケット配置分析

### 対象部材とループ変数
- （前回と同じ + 修正点があれば反映）

### ブラケット配置リスト
| # | Surfaces1 | Surfaces2 | BracketType | BaseElement | Sf1EndElements | 備考 |
|---|-----------|-----------|-------------|-------------|----------------|------|
| 1 | ... | ... | ... | ... | ... | ... |

### ループ構造方針
- ...

### パラメータ依存関係
- ...

### 注意事項
- ...
```

### `query.py` リトライループの変更

場所: `query.py` L1171-1216

変更前（現在）:
```python
if pipeline_mode == "three" and analysis_document:
    # ... エラーコンテキスト構築 ...
    fixed_bracket_section = generate_bracket_section_with_error(
        analysis_document, reference_code, error_ctx, llm=llm
    )
```

変更後:
```python
if pipeline_mode == "three" and analysis_document:
    # ... エラーコンテキスト構築（既存コード維持）...

    # === 新規: Agent 1 再分析 ===
    print("--- [Agent 1 Retry] Re-analyzing bracket placement ---")
    analysis_document = reanalyze_bracket_placement(
        instruction=original_instruction,  # 元の編集指示（ループ前に保持）
        original_code=original_code,
        previous_analysis=analysis_document,
        error_context=error_ctx,
        reference_code=reference_code,
        llm=llm,
    )
    # 再分析ドキュメントの保存
    if output_path:
        reanalysis_path = output_path.replace('.py', f'.reanalysis.attempt{attempt}.md')
        try:
            with open(reanalysis_path, 'w', encoding='utf-8') as f:
                f.write(analysis_document)
            print(f"--- Re-analysis saved to: {reanalysis_path} ---")
        except IOError as e:
            print(f"警告: 再分析ドキュメントの保存に失敗しました: {e}")

    # === Agent 2 は修正版 analysis_document を使用 ===
    fixed_bracket_section = generate_bracket_section_with_error(
        analysis_document, reference_code, error_ctx, llm=llm
    )
```

### `question` 変数の保持

場所: `query.py` L1090 付近（for attempt ループの直前）

`augmented_question` が `question` を上書きしている場合があるため、ループ前に保持:

```python
original_instruction = question  # Agent 1 再分析用に元の指示を保持
```

---

## 実装順序

### Step 1: 施策2（プロンプト追加） — 最小変更・即効性
1. `prompts/bracket_analysis_guide.md` にルール4追加
2. `prompts/bracket_analysis_guide_noref.md` にセクション8追加
3. テスト: 0310-2 の元スクリプトで再実行し、analysis.md に不正ペアが含まれないことを確認

### Step 2: 施策1（Agent 1 再実行） — フロー変更
1. `prompts/bracket_reanalysis.md` を新規作成
2. `prompts/loader.py` に以下4関数を追加:
   - `_extract_profile_name_map()`
   - `_detect_fore_aft_mismatch()`
   - `_build_reanalysis_error_section()`
   - `build_reanalysis_prompt()`
3. `query.py` に以下を追加:
   - `reanalyze_bracket_placement()` 関数
   - リトライループ内で Agent 1 再実行ロジック
   - `original_instruction` の保持
4. テスト: 意図的にエラーを起こし、再分析→修正→成功の一連フローを確認

### Step 3: 動作確認
- 0310-2 タイプ（Fore/Aft 不整合）のケースで再現テスト
- 0310-4 タイプ（同様の不整合）のケースで再現テスト
- 0310-3 タイプ（既に正しいケース）が劣化しないことを確認

---

## 成果物一覧

| 種別 | ファイル | 内容 |
|------|---------|------|
| 編集 | `prompts/bracket_analysis_guide.md` | ルール4追加 |
| 編集 | `prompts/bracket_analysis_guide_noref.md` | セクション8追加 |
| 新規 | `prompts/bracket_reanalysis.md` | Agent 1 リトライ用テンプレート |
| 編集 | `prompts/loader.py` | 4関数追加 |
| 編集 | `query.py` | `reanalyze_bracket_placement()` 追加 + リトライループ変更 |

## リスク・注意点

- Agent 1 再実行により **LLM呼び出し回数が増える**（リトライ1回あたり +1回）
  - ただし、Agent 2 だけの無駄なリトライ3回分より、Agent 1+2 の1回の方が効率的
- `_detect_fore_aft_mismatch()` は ProfileName の命名規則に依存
  - `Wall.Fore`/`Wall.Aft` と `.F`/`.FP`/`.A`/`.AP` のパターンが変わると要修正
- 再分析で候補数が減る可能性あり → 「20個以上」の要件を満たせない場合の対応が必要
  - 再分析プロンプトで「候補数の要件を維持するよう代替候補を提案する」指示を含めている
